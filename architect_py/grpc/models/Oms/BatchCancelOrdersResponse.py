# generated by datamodel-codegen:
#   filename:  Oms/BatchCancelOrdersResponse.json

from __future__ import annotations

from typing import List

from msgspec import Struct

from .. import definitions
from .Cancel import Cancel


class BatchCancelOrdersResponse(Struct, omit_defaults=True):
    cancel_rejects: List[definitions.CancelReject]
    cancels: List[Cancel]

    # Constructor that takes all field titles as arguments for convenience
    @classmethod
    def new(
        cls,
        cancel_rejects: List[definitions.CancelReject],
        cancels: List[Cancel],
    ):
        return cls(
            cancel_rejects,
            cancels,
        )

    def __str__(self) -> str:
        return f"BatchCancelOrdersResponse(cancel_rejects={self.cancel_rejects},cancels={self.cancels})"
